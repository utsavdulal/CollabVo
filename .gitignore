node_modules/
dist/
.vite/
uploads/
*.log
.DS_Store
Thumbs.db

# Environment files — NEVER commit
.env
.env.local
.env.*.local
!.env.example
!.env.production.example

# Build-time env files that must be included in the Docker image (public, non-secret)
!summer proj/admin-client/.env
!summer proj/client/.env
